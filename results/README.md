# Numerical outputs

Each notebook generates its own numerical arrays, diagnostics, and plots under this directory. The potential-game notebooks create timestamped run folders; other studies use named experiment folders. Final notebook cells can create ZIP archives of their outputs.

Generated runs and archives are excluded from Git. Keep their numerical settings and runtime metadata with the arrays when comparing experiments.
